<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="180"
        android:entries="@array/pref_listening_pause_time_titles"
        android:entryValues="@array/pref_listening_pause_time_values"
        android:key="listening_pause_time"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_listening_pause_time_title" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="-1"
        android:entries="@array/pref_listening_lang_titles"
        android:entryValues="@array/pref_listening_lang_values"
        android:key="listening_lang"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_listening_lang_title" />

</PreferenceScreen>
